version: '3'

services:
  app:
    build: 
      context: ./node
      dockerfile: Dockerfile
    container_name: app
    tty: true
    networks:
      - desafio-nginx
    ports:
      - "8080:8080"

networks:
  desafio-nginx:
    driver: bridge
